module.exports = {
    plugins: {
      // 1. Tailwind CSS processes the CSS directives (@tailwind)
      tailwindcss: {},
      // 2. Autoprefixer adds vendor prefixes for browser compatibility
      autoprefixer: {},
    },
  };